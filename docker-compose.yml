services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: snus
      POSTGRES_PASSWORD: snus
      POSTGRES_DB: sensordb
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
